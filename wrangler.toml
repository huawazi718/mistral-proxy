name = "unified-proxy"
main = "workers/index.js"
compatibility_date = "2024-01-01"

# Routes - 你可以根据需要修改
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]

# Environment variables - 需要在 CF Dashboard 中配置
# [vars]
# MISTRAL_API_KEY = ""
# NVIDIA_API_KEY = ""
# GEMINI_API_KEY = ""

# 或者���用 secrets (更安全)
# 通过 wrangler secret put MISTRAL_API_KEY 命令设置
# 或在 CF Dashboard > Workers > Settings > Variables 中添加